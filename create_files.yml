---
 - name: Create file
   copy:
      dest: /ansible/file1.txt
      content: |
        Text Line1, in file1
        Text Line2, in file1
        Text Line3, {{ mytext }}

 - name: Cteate file 2
   copy:
      dest: /ansible/file2.txt
      content: |
        Text Line1, in file2
        Text Line2, in file2
        Text Line3, {{ mytext }}

